# daqface
A high-level python interface for a number of common NI DAQmx hardware tasks.

## Installation
1. First install PyDAQmx with `pip install PyDAQmx`.
2. Run `python setup.py install` in the daqface directory.